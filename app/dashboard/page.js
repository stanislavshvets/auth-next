import css from "@/styles/dashboard.module.css";

const Dashboard = () => {
    return (
        <main className={css.main}>
            <h1>Dashboard</h1>
        </main>
    );
};

export default Dashboard;