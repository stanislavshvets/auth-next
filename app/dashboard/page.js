import css from "@/styles/dashboard.module.css";

const Dashboard = () => {
    return (
        <div className={css.main}>
            <h1>Dashboard</h1>
        </div>
    );
};

export default Dashboard;